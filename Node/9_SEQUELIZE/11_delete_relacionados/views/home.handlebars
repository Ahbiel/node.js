<h1>Usuários cadastrados:</h1>

<div>
    {{#each users}}
    <ul>
        <li>
            <a href="/users/{{this.id}}">Name: {{this.name}}</a> 
            - Profissão: {{this.occupation}}
            <a href="/users/edit/{{this.id}}"> - Editar</a> 
            <form action="/users/delete/{{this.id}}" method="POST">
                <input type="submit" value="Deletar">
            </form>
        </li>
    </ul>
    {{/each}}
</div>
